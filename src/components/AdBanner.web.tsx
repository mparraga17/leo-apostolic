// ============================================================
// AdBanner.web.tsx — STUB WEB (sin AdMob)
// ============================================================
// El SDK de Google Mobile Ads es solo nativo (iOS/Android) y no
// compila en web. En web no mostramos banner: este stub permite
// previsualizar la UI en el navegador sin cargar el módulo nativo.
// El componente nativo real vive en AdBanner.tsx.
// ============================================================

export default function AdBanner() {
  return null;
}
